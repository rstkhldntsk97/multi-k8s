sudo: required
services:
  - docker

before_install:
  - docker build -t rstkhldntsk/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true rstkhldntsk/docker-react npm run test -- --coverage
